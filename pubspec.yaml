name: bloc_timer
description: Timer app using Bloc.

version: 1.0.0+1

environment:
  sdk: ">=2.6.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^4.0.0
  equatable: ^1.0.0
  wave: ^0.0.8

flutter:
  uses-material-design: true
